# Sniffer 部署工具 — 功能说明（feature_new.md）

> 本文件在原 `feature.md` 基础上，合并了开发过程中新增/调整的所有功能，作为当前完整的功能说明。原 `feature.md` 的 7 条需求均已实现并扩展。

## 0. 概述与技术栈

- 平台：Windows 桌面工具软件。
- 用途：对局域网内的机器人（默认 `192.168.1.111`，用户 `robot`）通过 SSH/SCP 进行连接诊断、程序存在性检查、上传部署包并解压、运行/停止 `~/ats/sniffer` 程序、删除程序。
- 技术栈：Python 3.13 + CustomTkinter（界面）+ paramiko（SSH/SFTP）+ psutil（本机网卡枚举）。
- 交付形态：可用 `python main.py` 直接运行；也可用 PyInstaller 打包成单个 `sniffer_ui.exe`（无控制台窗口）。
- 界面风格：CustomTkinter 深色主题，自上而下分为「连接设置」「程序部署」「运行」「日志」四个区域。

---

## 1. 参数配置文件（config.json）—— 对应原需求 1

- 软件在 **exe 同目录**（开发期与 `main.py` 同目录）保存 `config.json`，记录上次使用的参数；启动时自动加载并回填到输入框。
- 保存的字段：`local_ip`、`target_ip`、`port`、`username`、`password`、`last_archive_dir`（上次选的压缩包目录）、`run_command`（运行命令）。
- 保存时机：任意参数变更/窗口关闭时写回；文件缺失或损坏时用默认值重建。
- 密码以**明文**存储（内网部署工具，便利优先）。

### 1.1 默认配置文件（default_config.json）与「恢复默认配置」按钮（新增）

- 同目录下有 `default_config.json`，内容为"出厂默认值"，**用户可直接编辑它**来定制自己的默认参数。
- 「连接设置」区有「恢复默认配置」按钮：点击后把 `default_config.json` 复制到 `config.json`（覆盖当前配置），重新加载所有输入框，并断开当前（已失效的）连接、状态回到"未连接"、提示"请重新连接"。
- 若 `default_config.json` 不存在（被删等），程序会自动从内置默认值/打包资源重建它，按钮始终可用。
- 打包成 exe 时，`default_config.json` 会一并打入；首次运行时自动释放到 exe 同目录，方便编辑。

---

## 2. 本地 IP 下拉选择 —— 对应原需求 2（增强）

- 「连接设置」区有本地 IP 下拉框，列出本机所有**启用中**网卡的 IPv4 地址。
- 枚举方式：通过 `psutil` 实时读取网卡状态（**禁用/断开的网卡不显示**，也排除回环 `127.x` 和无用的链路本地 `169.254.x`）。相比早期基于 `gethostbyname` 的实现，不会出现已禁用网卡 IP 残留的问题。
- 下拉框旁边有 **↻ 刷新按钮**：点击后重新扫描本机网卡并更新列表（插拔网线/切换网络后点一下即可刷新）。当前选中的 IP 若仍存在则保持选中。
- 选中策略：
  - 若 `config.json` 里存过 `local_ip` 且仍存在 → 用存的那个。
  - 否则**优先选与目标 IP 同网段的网卡**（例如目标 `192.168.1.115` → 优先选 `192.168.1.xxx`）；没有同网段的则选第一个。

---

## 3. 目标连接参数 —— 对应原需求 3

- 输入框可修改：目标 IP（默认 `192.168.1.111`）、端口（默认 `22`）、用户名（默认 `robot`）、密码（默认 `MangoTango`，输入框默认掩码 `*`，可勾选「显示」查看）。
- 这些参数都会保存到 `config.json`，下次启动自动回填。

---

## 4. 连接按钮 —— 对应原需求 4

- 点击「连接」：用 SSH（paramiko，密码认证，`AutoAddPolicy` 自动接受主机指纹）连接目标 IP。
- 连接结果在日志区与状态点显示：成功→状态点变绿"已连接"；失败→日志打印中文原因（如超时、认证失败、网络不可达），状态点保持红色，连接按钮可重新点击重试。
- 连接成功后**自动检查**目标机上 `~/ats/sniffer` 是否存在，结果用指示灯显示：✅ 已存在 / ❌ 不存在。
- 所有 SSH 操作在后台线程执行，界面不会卡顿。

---

## 5. 上传并解压按钮 —— 对应原需求 5（增强）

- 「程序部署」区：输入框 + 「浏览」按钮选择电脑本地压缩包，「上传并解压」按钮执行。
- 点击后：用 SFTP 把压缩包拷贝到目标机 `~/ats/` 目录，并**按扩展名自动解压**：
  - `.tar.gz` / `.tgz` → `tar -xzf`
  - `.tar` → `tar -xf`
  - `.zip` → `unzip -o`
  - 其他格式 → 日志报错"不支持的压缩格式"，不做上传。
- 解压后自动重新检查 `~/ats/sniffer` 是否存在并更新指示灯。
- 每一步（上传、解压、检查）的进度/结果写入日志。

---

## 6. 运行 / 停止按钮 —— 对应原需求 6（大幅增强）

### 6.1 运行命令可编辑并保存（新增）
- 「运行」区有「运行命令」输入框，默认显示 `sudo ~/ats/sniffer --bin`，用户可直接修改（换参数、换路径、或完全换命令）。
- 该命令保存到 `config.json` 的 `run_command`，下次启动自动回填。
- 点击「运行」即用输入框中的命令执行。

### 6.2 启动前预检（新增）
- 运行前会从命令中解析出二进制路径（跳过 `sudo` 及其参数），`test -f` 检查它在不在；不在则直接报 `[运行失败] <路径> 不存在`，不启动。
- 命令不是路径形式（如 `ls`、`systemctl`）时跳过预检，直接运行。

### 6.3 运行与停止
- 点击「运行」：通过交互式 shell（PTY）下发命令；若 `sudo` 要求密码，自动用登录密码回填。
- **运行成功**：按键立即变为「停止」，日志打印 `[运行成功] <命令> 已启动`；sniffer 的实时输出（含颜色码已被过滤，显示干净文本）滚动显示在日志区。
  - 启动后有约 1.2 秒确认窗口：若程序一启动就退出，则报 `[运行失败] 程序启动后立即退出` 并把按键恢复为「运行」。
  - 命令是在 shell 就绪（收到第一段输出）后才下发，避免某些 SSH 服务丢命令。
- **点击「停止」**：向同一通道发送 Ctrl+C（`\x03`）退出程序，并关闭通道、重置状态，按键恢复为「运行」，连接/上传/删除等按钮重新可用。
- **失败**：连接未建立、命令为空、启动异常等情况，日志返回结果，不崩溃。

---

## 7. 删除按钮 —— 对应原需求 7

- 「运行」区有「删除 ~/ats/sniffer」按钮：点击后执行 `rm -f ~/ats/sniffer`，删除目标机上的程序。
- 删除后自动重新检查并更新指示灯（→ ❌ 不存在），结果写入日志。

---

## 8. 实时日志控制台（新增）

- 底部为滚动日志区，实时显示：命令执行结果、连接/上传/解压/运行/删除各步骤的中文提示、sniffer 程序的实时输出。
- 远端输出中的 ANSI 颜色/控制码（如 `ESC[1;34m`）会被自动过滤，日志显示干净文本。
- 任何异常（网络、SSH、内部错误）都转成中文提示写入日志，绝不弹原生异常、绝不崩溃。

---

## 9. 程序检查刷新（新增）

- 「程序部署」区「程序检查 ~/ats/sniffer」指示灯旁有 **↻ 刷新按钮**：点击后重新执行 `test -f ~/ats/sniffer` 检查程序是否还在（删除或重新上传后点一下即可刷新状态），无需重连。

---

## 10. 打包与运行

- 开发运行：`python main.py`。
- 打包：`python -m PyInstaller sniffer_ui.spec --noconfirm --clean`，产物为 `dist/sniffer_ui.exe`（单文件、无控制台窗口，已包含 customtkinter 资源、paramiko、psutil、default_config.json）。
- 首次运行 exe 时，`config.json` 与 `default_config.json` 会生成在其同目录。

---

## 附：原 feature.md 需求对照

| 原 feature.md | 现状 |
|---|---|
| 1. 配置文件记忆上次参数 | ✅ `config.json`（exe 同目录，明文）+ `default_config.json` + 恢复默认按钮 |
| 2. 下拉菜单选本地 IP | ✅ psutil 实时枚举（禁用网卡不显示）+ ↻刷新 + 优先选目标同网段 |
| 3. 目标 IP/用户 robot/密码 MangoTango 可改 | ✅ 输入框可改，密码可显示/隐藏，全部保存 |
| 4. 连接按钮 + 自动检查 ~/ats/sniffer | ✅ SSH 密码认证，成功后自动检查，指示灯显示 |
| 5. 上传按钮(选压缩包) scp 到 ~/ats 并解压 | ✅ SFTP 上传 + 按扩展名自动解压 + 检查刷新 |
| 6. 运行 sudo ~/ats/sniffer --bin，成功变停止，再点 Ctrl+C | ✅ 命令可编辑保存 + 预检 + 成功变停止 + Ctrl+C 停止 + 实时日志 + 颜色码过滤 |
| 7. 删除 ~/ats/sniffer | ✅ 删除 + 检查刷新 |

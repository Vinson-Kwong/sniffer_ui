写一个windows的桌面工具软件，功能需求如下：
1、软件保存一个参数配置文件，记录上一次使用的参数，软件使用前加载参数。
2、搞一个下拉菜单，选择一个本地ip作为连接。
3、输入连接的目标参数ip:192.168.1.111,用户默认参数robot，密码默认参数MangoTango，这些参数可以在输入框修改。
4、做一个叫连接的按钮，点击后，ssh连接目标IP,连接返回结果。连接成功后，自动检查目标ip是否有后面需要运行的程序，程序路径是~/ats/sniffer，检查返回结果。
5、做一个叫上传的按钮，按钮旁边可以选择电脑本地的压缩包文件。点击按钮后，scp把选择文件拷贝到目标ip的~/ats目录，并自动解压。
6、做一个叫运行的按钮，点击后，运行程序命令：sudo ~/ats/sniffer --bin，如果失败，返回结果。如果运行成功，显示为停止，此时再点击一下，执行Ctrl+C的操作退出程序。
7、做一个叫删除的按钮，点击后，删除程序 ~/ats/sniffer。
